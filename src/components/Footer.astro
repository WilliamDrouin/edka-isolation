---
import { mainNav } from '../lib/navigation';

const year = new Date().getFullYear();

const services = [
  { label: 'Uréthane giclé', href: '/services#urethane' },
  { label: 'Cellulose soufflée', href: '/services#cellulose' },
  { label: 'Fibre de verre', href: '/services#fibre-verre' },
  { label: 'Isolation des combles', href: '/services#combles' },
  { label: 'Isolation de sous-sol', href: '/services#sous-sol' },
] as const;

const coordonnees = {
  ville: 'Saint-Georges-de-Beauce, QC',
  region: 'Service en Chaudière-Appalaches et environs',
  telephone: '(418) 000-0000',
  courriel: 'info@isolation-edka.ca',
  rbq: 'RBQ : à confirmer',
} as const;
---

<footer class="dark bg-background text-foreground">
  <div class="container-page py-16">
    <div class="grid gap-10 lg:grid-cols-4">
      <div class="lg:col-span-1">
        <a href="/" class="inline-flex items-center" aria-label="Accueil — Isolation EDKA">
          <img src="/images/logoprincipale.jpg" alt="Isolation EDKA" class="h-14 w-auto" />
        </a>
        <p class="text-muted-foreground mt-4 text-sm leading-relaxed">
          Spécialiste en isolation à Saint-Georges-de-Beauce. Performance thermique, étanchéité,
          subventions admissibles.
        </p>
      </div>

      <div>
        <h2 class="font-display text-sm font-semibold tracking-wide uppercase">Navigation</h2>
        <ul class="mt-4 space-y-2 text-sm">
          {
            mainNav.map((item) => (
              <li>
                <a
                  href={item.href}
                  class="text-muted-foreground hover:text-foreground transition-colors"
                >
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h2 class="font-display text-sm font-semibold tracking-wide uppercase">Services</h2>
        <ul class="mt-4 space-y-2 text-sm">
          {
            services.map((s) => (
              <li>
                <a
                  href={s.href}
                  class="text-muted-foreground hover:text-foreground transition-colors"
                >
                  {s.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h2 class="font-display text-sm font-semibold tracking-wide uppercase">Coordonnées</h2>
        <address class="text-muted-foreground mt-4 space-y-2 text-sm not-italic">
          <p>{coordonnees.ville}</p>
          <p>{coordonnees.region}</p>
          <p>
            <a
              href={`tel:${coordonnees.telephone.replace(/\D/g, '')}`}
              class="hover:text-foreground transition-colors"
            >
              {coordonnees.telephone}
            </a>
          </p>
          <p>
            <a href={`mailto:${coordonnees.courriel}`} class="hover:text-foreground transition-colors">
              {coordonnees.courriel}
            </a>
          </p>
          <p class="text-muted-foreground/70 pt-2 text-xs">{coordonnees.rbq}</p>
        </address>
      </div>
    </div>

    <div
      class="border-border/40 text-muted-foreground mt-12 flex flex-col gap-4 border-t pt-6 text-xs sm:flex-row sm:items-center sm:justify-between"
    >
      <p>© {year} Isolation EDKA. Tous droits réservés.</p>
      <p>
        Site fait avec
        <span class="text-[color:var(--color-brand-400)]">soin</span>
        en Beauce.
      </p>
    </div>
  </div>
</footer>
